export interface Wallet {
    id:number,
    uuid: string;
    address: string;
    walletObj: string;
  }
export interface User {
    id:number,
    uuid: string;
    uname: string;
    email: string;
    passwd: string;
}